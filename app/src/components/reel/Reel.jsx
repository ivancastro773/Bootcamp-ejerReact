import React from "react";
import * as Yup from "yup";
import { Formik, Form, Field } from "formik";

const initialState = { email: "", name: "", age: 0, color: "" };
export default Reel = () => {
  return <div>hola</div>;
};
